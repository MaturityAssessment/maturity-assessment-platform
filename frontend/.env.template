# Copy this template to `.env.local` for local development. These NEXT_PUBLIC
# values are embedded in browser code and must never contain secrets.
NEXT_PUBLIC_API_URL=http://localhost:8080

# These are localStorage key names, not authentication tokens.
NEXT_PUBLIC_TOKEN=auth_token
NEXT_PUBLIC_REFRESH_TOKEN=refresh_token
